#include <stdio.h>

int main() {
    char str[] = "Hello, World!";
    int sum = 0;

    for (int i = 0; str[i] != '\0'; i++) {
        sum += str[i];
    }

    printf("The sum of characters in the string is: %d\n", sum);
    return 0;
}
